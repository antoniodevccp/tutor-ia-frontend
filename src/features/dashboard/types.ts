export type DashboardTopic = {
  id: number
  name: string
  materialVersion: number
  contextNeedsRefresh: boolean
  subjectId: number
  subjectName: string
  gradeLevelId: number
  gradeLevelName: string
}